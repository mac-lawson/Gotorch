package matrix

import "fmt"

func Add(matrixA *Matrix, matrixB *Matrix) {
	// returnMatrix := Matrix{
	// 	Internal: [][]float64{},
	// }

	if len(matrixA.Internal) != len(matrixB.Internal) {
		fmt.Println("nope")
	} else {

	}
}
